// This is an example of a Rust function that takes two numbers and returns their sum.
fn add(a: i32, b: i32) -> i32 {
    a + b
}

// You can call this function with different inputs to get different results. For example:
let result = add(4, 5); // result is 9
let result = add(-3, 2); // result is 1
